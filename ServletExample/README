Первый сервлет, запущен из IDEA на Tomcat, собран на Maven(без архитипа)
Преход по URL, сервлет распалсил запрос, дальше ищет web.xml, определяет имя сервлета, ищет путь к нему, 
дальше идет в класс с именем сервлета и выполняет код

Передача проходит без .JSP, посредством response

HttpServletRequest req – это запрос, который пришел к сервлету;

HttpServletResponse resp – это ответ который даст сервлет.
